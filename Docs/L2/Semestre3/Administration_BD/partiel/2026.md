# PARTIEL MARS 2026 — IMPLEMENTATION DES BASES DE DONNEES & ADMINISTRATION DES BASES DE DONNEES LIBRES

::::{grid}
:gutter: 2

:::{grid-item}
:columns: 12

**Cycle des Ingénieurs des Travaux Informatiques**  
**Licence Professionnelle en Informatique — Tronc Commun**

**Institut Africain d'Informatique (IAI-TOGO)**  
Établissement Inter-États d’Enseignement Supérieur  
Représentation du Togo (IAI-TOGO)  
07 BP : 12456 Lomé 07  
Tél : (+228) 22 22 12 07  
E-mail : info@iai-togo.tg  
Site Web : www.iai-togo.tg

**Chargés de cours :** MM. TIDJANI/TCHANTCHO, M. HOUNGBO

**Date :** 06/03/2026  
**Durée :** 2H00mn  
**Filière :** TC2  
**Semestre :** III
:::
::::

:::{important}
**NB :** Documents et calculatrice non autorisés.
:::

Le sujet comporte deux parties **A** et **B** indépendantes de questions à choix multiples (**QCM**).

Pour chaque question, le candidat choisira uniquement la lettre de la réponse qu’il juge correcte. Toute autre action équivaut à zéro **(0)**.

## PARTIE A : QUESTIONS DE COURS (10 points)

1. **Dans MySQL, quel moteur de stockage supporte les transactions ACID ?**

   A. MyISAM  
   B. MEMORY  
   C. InnoDB  
   D. CSV

2. **Dans PostgreSQL, quel composant gère la concurrence via MVCC ?**

   A. WAL  
   B. Autovacuum  
   C. MVCC interne  
   D. Buffer Manager

3. **Que signifie ACID ?**

   A. Atomicité, Cohérence, Isolation, Durabilité  
   B. Accessibilité, Cohérence, Isolation, Données  
   C. Atomicité, Centralisation, Isolation, Distribution  
   D. Analyse, Contrôle, Indexation, Durabilité

4. **Quel est le rôle du WAL dans PostgreSQL ?**

   A. Optimiser les requêtes  
   B. Assurer la journalisation des transactions  
   C. Gérer les index  
   D. Nettoyer les tables

5. **Dans MySQL, quelle isolation évite les lectures fantômes ?**

   A. READ UNCOMMITTED  
   B. READ COMMITTED  
   C. REPEATABLE READ  
   D. SERIALIZABLE

6. **PostgreSQL utilise par défaut quel niveau d’isolation ?**

   A. READ UNCOMMITTED  
   B. READ COMMITTED  
   C. SERIALIZABLE  
   D. REPEATABLE READ

7. **Quel mécanisme permet d’éviter les blocages en lecture dans PostgreSQL ?**

   A. Index B-tree  
   B. MVCC  
   C. Triggers  
   D. Partitionnement

8. **Dans MySQL, quel moteur ne supporte pas les clés étrangères ?**

   A. InnoDB  
   B. MyISAM  
   C. NDB  
   D. ARCHIVE

9. **Dans PostgreSQL, que fait VACUUM ?**

   A. Supprime les tables  
   B. Nettoie les tuples morts  
   C. Crée des index  
   D. Sauvegarde la base

10. **Quel est l’avantage principal du MVCC ?**

    A. Augmenter la taille des données  
    B. Éviter les verrous de lecture  
    C. Supprimer les index  
    D. Réduire la sécurité

11. **Quelle clause permet de filtrer après agrégation ?**

    A. WHERE  
    B. GROUP BY  
    C. HAVING  
    D. ORDER BY

12. **Quelle commande analyse les performances d’une requête dans PostgreSQL ?**

    A. ANALYZE  
    B. EXPLAIN  
    C. SHOW  
    D. PLAN

13. **Dans MySQL, EXPLAIN sert à :**

    A. Supprimer les tables  
    B. Analyser le plan d’exécution  
    C. Créer un index  
    D. Lancer une transaction

14. **Quelle structure accélère les recherches ?**

    A. Vue  
    B. Index  
    C. Trigger  
    D. Transaction

15. **Quel type d’index est utilisé par défaut dans PostgreSQL ?**

    A. Hash  
    B. B-tree  
    C. GIN  
    D. GiST

16. **GIN est utilisé pour :**

    A. Les clés étrangères  
    B. Les données textuelles et JSON  
    C. Les transactions  
    D. Les vues

## PARTIE B : PRATIQUES (10 points)

17. **Une vue matérialisée :**

    A. Est recalculée à chaque requête  
    B. Stocke physiquement les données  
    C. Est temporaire  
    D. N’existe pas en PostgreSQL

18. **Quelle commande met à jour une vue matérialisée ?**

    A. UPDATE VIEW  
    B. REFRESH MATERIALIZED VIEW  
    C. ALTER VIEW  
    D. REBUILD VIEW

19. **Une sous-requête corrélée :**

    A. Est indépendante  
    B. Dépend de la requête principale  
    C. Est toujours plus rapide  
    D. Ne peut pas être utilisée dans SELECT

20. **Quel opérateur teste l’existence d’un résultat ?**

    A. IN  
    B. EXISTS  
    C. ANY  
    D. ALL

## Modèle relationnel : gestion des projets informatiques

- `EMPLOYE (id_employe, nom, prenom, email, poste)`
- `PROJET (id_projet, nom_projet, date_debut, date_fin, budget)`
- `EQUIPE (id_equipe, nom_equipe)`
- `TACHE (id_tache, libelle, date_debut, date_fin, statut, id_projet#)`
- `AFFECTATION (id_employe#, id_projet#, role, date_affectation)`
- `PARTICIPATION (id_employe#, id_tache#, nb_heures)`
- `APPARTENIR (id_employe#, id_equipe#)`

## TRAVAIL À FAIRE

1. **Quels employés ont travaillé sur un projet dont le budget dépasse 10 000 000 ?**

   **A)**
   ```sql
   SELECT e.*
   FROM EMPLOYE e
   JOIN PROJET p ON e.id_employe = p.id_projet
   WHERE p.budget > 10000000;
   ```

   **B)**
   ```sql
   SELECT DISTINCT e.*
   FROM EMPLOYE e
   JOIN AFFECTATION a ON e.id_employe = a.id_employe
   JOIN PROJET p ON a.id_projet = p.id_projet
   WHERE p.budget > 10000000;
   ```

   **C)**
   ```sql
   SELECT *
   FROM EMPLOYE
   WHERE budget > 10000000;
   ```

   **D)**
   ```sql
   SELECT *
   FROM EMPLOYE e
   JOIN TACHE t ON e.id_employe = t.id_projet;
   ```

2. **Quelle requête liste les projets sans tâches ?**

   **A)**
   ```sql
   SELECT *
   FROM PROJET p
   LEFT JOIN TACHE t ON p.id_projet = t.id_projet
   WHERE t.id_tache IS NULL;
   ```

   **B)**
   ```sql
   SELECT *
   FROM PROJET
   WHERE id_projet NOT IN (
       SELECT id_projet
       FROM TACHE
   );
   ```

   **C)** Les deux sont correctes

   **D)** Aucune

3. **Quelle requête identifie le projet au budget maximal ?**

   **A)**
   ```sql
   SELECT MAX(budget)
   FROM PROJET;
   ```

   **B)**
   ```sql
   SELECT id_projet
   FROM PROJET
   ORDER BY budget DESC
   LIMIT 1;
   ```

   **C)**
   ```sql
   SELECT *
   FROM PROJET
   WHERE budget = MAX(budget);
   ```

   **D)** Aucune

4. **Quels employés ont travaillé sur tous les projets ?**

   **A)**
   ```sql
   SELECT id_employe
   FROM AFFECTATION
   GROUP BY id_employe;
   ```

   **B)**
   ```sql
   SELECT e.id_employe
   FROM EMPLOYE e
   WHERE NOT EXISTS (
       SELECT p.id_projet
       FROM PROJET p
       WHERE NOT EXISTS (
           SELECT *
           FROM AFFECTATION a
           WHERE a.id_employe = e.id_employe
             AND a.id_projet = p.id_projet
       )
   );
   ```

   **C)**
   ```sql
   SELECT id_employe
   FROM AFFECTATION
   GROUP BY id_employe
   HAVING COUNT(*) = (
       SELECT COUNT(*)
       FROM PROJET
   );
   ```

   **D)** B et C

5. **Quels projets ont un budget supérieur au budget moyen ?**

   **A)**
   ```sql
   SELECT *
   FROM PROJET
   WHERE budget > AVG(budget);
   ```

   **B)**
   ```sql
   SELECT *
   FROM PROJET
   WHERE budget > (
       SELECT AVG(budget)
       FROM PROJET
   );
   ```

   **C)**
   ```sql
   HAVING budget > AVG(budget);
   ```

   **D)**
   ```sql
   SELECT MAX(budget)
   FROM PROJET;
   ```

6. **Quelle requête identifie les projets dont toutes les tâches sont terminées ?**

   **A)**
   ```sql
   SELECT id_projet
   FROM TACHE
   GROUP BY id_projet
   HAVING SUM(statut = 'Terminé') = COUNT(*);
   ```

   **B)**
   ```sql
   SELECT id_projet
   FROM TACHE
   WHERE statut = 'Terminé';
   ```

   **C)**
   ```sql
   WHERE statut <> 'Terminé';
   ```

   **D)**
   ```sql
   HAVING statut = 'Terminé';
   ```

7. **Quelle requête trouve les projets sans employés affectés ?**

   **A)**
   ```sql
   SELECT *
   FROM PROJET
   WHERE id_projet NOT IN (
       SELECT id_projet
       FROM AFFECTATION
   );
   ```

   **B)**
   ```sql
   SELECT *
   FROM PROJET
   JOIN AFFECTATION;
   ```

   **C)**
   ```sql
   SELECT *
   FROM PROJET
   WHERE id_employe IS NULL;
   ```

   **D)** Impossible

8. **Quelle requête permet d’identifier l’équipe ayant le plus grand nombre de membres ?**

   **A)**
   ```sql
   SELECT id_equipe, COUNT(*)
   FROM APPARTENIR
   GROUP BY id_equipe
   ORDER BY COUNT(*) DESC
   LIMIT 1;
   ```

   **B)**
   ```sql
   SELECT id_equipe
   FROM APPARTENIR
   WHERE MAX(id_equipe);
   ```

   **C)**
   ```sql
   SELECT id_equipe
   FROM APPARTENIR
   HAVING COUNT(*) > 1;
   ```

   **D)**
   ```sql
   SELECT COUNT(id_equipe)
   FROM APPARTENIR;
   ```

9. **Quelle requête calcule correctement la durée moyenne des tâches par projet ?**

   **A)**
   ```sql
   SELECT AVG(DATEDIFF(date_fin, date_debut))
   FROM TACHE;
   ```

   **B)**
   ```sql
   SELECT id_projet, AVG(DATEDIFF(date_fin, date_debut))
   FROM TACHE
   GROUP BY id_projet;
   ```

   **C)**
   ```sql
   SELECT id_projet, DATEDIFF(date_fin, date_debut)
   FROM TACHE;
   ```

   **D)**
   ```sql
   SELECT AVG(DATEDIFF(date_fin, date_debut))
   FROM TACHE
   HAVING AVG(DATEDIFF(date_fin, date_debut));
   ```

10. **Que retourne cette requête ?**

    ```sql
    SELECT e.nom
    FROM EMPLOYE e
    LEFT JOIN PARTICIPATION p ON e.id_employe = p.id_employe
    WHERE p.id_tache IS NULL;
    ```

    A. Les employés ayant participé à au moins une tâche  
    B. Les employés sans participation à une tâche  
    C. Tous les employés  
    D. Les employés avec `NULL` comme identifiant

11. **Quel est le résultat ?**

    ```sql
    SELECT t.id_projet, SUM(p.nb_heures)
    FROM TACHE t
    JOIN PARTICIPATION p ON t.id_tache = p.id_tache
    GROUP BY t.id_projet;
    ```

    A. Total des heures par tâche  
    B. Total des heures par projet  
    C. Nombre de tâches par projet  
    D. Liste des employés

12. **Que retourne cette requête ?**

    ```sql
    SELECT nom_projet
    FROM PROJET
    WHERE budget > (
        SELECT AVG(budget)
        FROM PROJET
    );
    ```

    A. Tous les projets  
    B. Les projets avec budget minimum  
    C. Les projets au-dessus du budget moyen  
    D. Les projets sans budget

13. **Que retourne cette requête ?**

    ```sql
    SELECT *
    FROM TACHE
    WHERE DATEDIFF(date_fin, date_debut) > 30;
    ```

    A. Tâches courtes  
    B. Tâches longues (> 30 jours)  
    C. Toutes les tâches  
    D. Tâches sans dates